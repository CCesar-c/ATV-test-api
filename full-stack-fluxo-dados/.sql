-- CREATE TABLE perfis (
--     id SERIAL PRIMARY KEY,
--     nome VARCHAR(100) NOT NULL,
--     foto_url VARCHAR(255), -- Armazena o caminho relativo local, por exemplo: "/uploads/foto-123.jpg"
--     curriculo_url VARCHAR(255) -- Armazena o caminho relativo do documento, por exemplo: "/uploads/cv-123.pdf"
-- );

select * from perfis